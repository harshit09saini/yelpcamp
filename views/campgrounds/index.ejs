<% include ../partials/header %>

<div class="container">
    <header class="jumbotron">
        <div class="container">
            <h1>Welcome to YelpCamp!</h1>
            <p>View our hand-picked campgrounds from all over the world!</p>
            <p>
                    <a class="btn btn-primary btn-large" href="/campgrounds/new">Add a New Campground</a>
            </p>
        </div>
    </header>


<!-- <div class="card-deck">
    <div class="col-md-3 col-sm-6">
        <% for(var i=0; i<campgrounds.length; i++) {%>
            <div class="card" style="width:300px">
                <img class="card-img-top" src="<%= campgrounds[i].image %>" alt="Card image" style="width:100%">
                <div class="card-body">
                    <h4 class="card-title"><%= campgrounds[i].name %></h4>
                    <p class="card-text"></p>
                    <a href="/campgrounds/<%= campgrounds[i]._id %>" class="btn btn-primary">More Info</a>
                </div>
        </div>
        <% } %>
    </div>
</div> -->



<div class="row" style="display: flex; flex-wrap: wrap;">
        <% for(var i=0; i<campgrounds.length; i++) {%>
            <div class="col-md-3 col-sm-6">
                <div class="img-thumbnail">
                    <img src="<%= campgrounds[i].image %>" style="width: 100%"> <!-- added style tag to fit images in the thumbnail container -->
                </div>
                <div>
                    <figcaption class="figure-caption text-center"><strong><%= campgrounds[i].name %></strong></figcaption>
                </div>
                <div>
                    <a href="/campgrounds/<%= campgrounds[i]._id %>" class="btn btn-primary">More Info</a>
                </div>
            </div>
        <% } %>
    </div>
</div>


<% include ../partials/footer %>
